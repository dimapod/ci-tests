language: node_js
node_js:
  - 0.8

before_script:
  - export DISPLAY=:99.0
  - gem install compass
  - sh -e /etc/init.d/xvfb start
  - npm -d install --silent

script:
  - webdriver-manager update
  - npm run-script test_drone
